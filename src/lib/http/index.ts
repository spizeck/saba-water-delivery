/**
 * HTTP API boundary utilities. `withApiRoute` is the canonical route wrapper —
 * see TECHNICAL.md "Server error handling".
 */

export { withApiRoute, type WithApiRouteOptions } from "./apiRoute";
